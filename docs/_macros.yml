version: 2

macros:
  - name: parse_date
    description: >
      Parses a date value from different date strings
    
    arguments:
      - name: column
        type: string
        description: The name of the column containing dates